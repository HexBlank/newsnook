# 墨水屏模式（einkMode）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现完整可交付的 `einkMode`：单开关叠加行为（关动画、弱化装饰、文章分页），关闭后与当前正常模式比特级一致。

**Architecture:** `Preferences.einkMode` 与 `theme` 正交；运行时写 `document.documentElement.dataset.eink`；`useReducedMotion` 在 eink 时恒 true；CSS `[data-eink='1']` 压制动效与装饰；Reader 在 eink 时走独立分页分支，滚动分支保持不动。

**Tech Stack:** React 19、TypeScript、现有 preferences / theme / CSS token、`ToggleSwitch`、`node:assert` + rolldown/tsx 脚本测试（与仓库现有 `test:*` 一致）

## Global Constraints

- 规格：`docs/superpowers/specs/2026-08-11-eink-mode-design.md`
- **`einkMode === false`（默认）零回归**：不得改变现有动效、装饰、滚动 Reader、主题切换过渡
- 颜色仍跟随 `theme`；**不**新增 `theme: 'eink'`
- **零子配置**：仅 `einkMode` 布尔；开即全套，关即全撤
- 图/视频策略不变
- 不自动检测墨水屏设备
- 未经用户明确要求不 `git push`；每任务末按步骤 commit

---

## File Structure

| 文件 | 职责 |
|---|---|
| `src/lib/eink.ts` | `applyEinkMode` / `isEinkModeActive`；与 theme 同级的 DOM 副作用 |
| `src/sources/preferences.ts` | `einkMode` 字段、默认值、归一化、`setEinkMode` |
| `src/hooks/usePreferences.ts` | prefs 变化时调用 `applyEinkMode`；主题 animate 受 eink 门闩 |
| `src/hooks/useReducedMotion.ts` | 系统 reduce **或** `data-eink='1'` |
| `src/index.css` | `[data-eink='1']` 动效压制 + 装饰 token 覆盖 |
| `index.html` | 启动脚本同步 `data-eink` 防闪 |
| `src/screens/settings/AppearanceScreen.tsx` | 「墨水屏模式」开关 UI |
| `src/App.tsx` | 向 Appearance / Reader 传递 `einkMode` |
| `src/lib/readerPagination.ts` | 点击分区、装页、锚点页码纯函数 |
| `src/screens/ReaderScreen.tsx` | eink 时分页分支；非 eink 保持现有滚动实现 |
| `scripts/eink-mode.test.ts` | 偏好归一化 + applyEinkMode + pagination 单测 |
| `package.json` | `test:eink` script |

---

### Task 1: 偏好字段 `einkMode` + 归一化 + setter

**Files:**
- Modify: `src/sources/preferences.ts`
- Create: `scripts/eink-mode.test.ts`（本任务先写 prefs 部分；后续任务追加）
- Modify: `package.json`（增加 `test:eink`）

**Interfaces:**
- Produces:
  - `Preferences.einkMode: boolean`
  - `DEFAULT_PREFERENCES.einkMode === false`
  - `setEinkMode(prefs: Preferences, enabled: boolean): Preferences`
  - `normalizePreferences`：缺省 / 非 boolean → `false`

- [ ] **Step 1: 写失败单测（prefs 部分）**

创建 `scripts/eink-mode.test.ts`:

```ts
import assert from 'node:assert/strict'

import {
  DEFAULT_PREFERENCES,
  normalizePreferences,
  setEinkMode,
} from '../src/sources/preferences'

assert.equal(DEFAULT_PREFERENCES.einkMode, false)

assert.equal(normalizePreferences({}).einkMode, false)
assert.equal(normalizePreferences({ einkMode: true }).einkMode, true)
assert.equal(normalizePreferences({ einkMode: 'yes' as unknown as boolean }).einkMode, false)

const on = setEinkMode(DEFAULT_PREFERENCES, true)
assert.equal(on.einkMode, true)
assert.equal(on.theme, DEFAULT_PREFERENCES.theme)
assert.equal(setEinkMode(on, true), on)
assert.equal(setEinkMode(on, false).einkMode, false)

console.log('eink-mode prefs: ok')
```

- [ ] **Step 2: 跑测确认失败**

Run:

```bash
npx tsx scripts/eink-mode.test.ts
```

Expected: FAIL（`einkMode` 不存在 / undefined）

- [ ] **Step 3: 最小实现**

在 `Preferences` 接口增加 `einkMode: boolean`。

在 `DEFAULT_PREFERENCES` 增加 `einkMode: false`。

在 `normalizePreferences` 的 return 对象中增加：

```ts
einkMode: typeof input.einkMode === 'boolean' ? input.einkMode : false,
```

在文件末尾（靠近 `setThemeMode`）增加：

```ts
export function setEinkMode(prefs: Preferences, enabled: boolean): Preferences {
  return prefs.einkMode === enabled ? prefs : { ...prefs, einkMode: enabled }
}
```

- [ ] **Step 4: 跑测确认通过**

Run:

```bash
npx tsx scripts/eink-mode.test.ts
```

Expected: `eink-mode prefs: ok`

在 `package.json` scripts 增加：

```json
"test:eink": "npx tsx scripts/eink-mode.test.ts"
```

- [ ] **Step 5: Commit**

```bash
git add src/sources/preferences.ts scripts/eink-mode.test.ts package.json
git commit -m "feat(prefs): add einkMode preference flag"
```

---

### Task 2: `applyEinkMode` + 启动防闪 + `usePreferences` 同步

**Files:**
- Create: `src/lib/eink.ts`
- Modify: `index.html`（preferences 防闪 IIFE）
- Modify: `src/hooks/usePreferences.ts`
- Modify: `scripts/eink-mode.test.ts`（追加 DOM 单测；Node 下用最小 mock 或只测纯逻辑）

**Interfaces:**
- Consumes: `Preferences.einkMode`
- Produces:
  - `applyEinkMode(enabled: boolean): void` — `enabled` 时 `dataset.eink = '1'`，否则 `delete dataset.eink`
  - `isEinkModeActive(): boolean` — `document.documentElement.dataset.eink === '1'`

- [ ] **Step 1: 写失败单测（append）**

在 `scripts/eink-mode.test.ts` 末尾追加（tsx 跑在 Node，用简单 document mock）：

```ts
import { applyEinkMode, isEinkModeActive } from '../src/lib/eink'

// jsdom 不可用时：若全局无 document，跳过 DOM 断言并打印提示；
// 本仓库测试以模块导出存在 + 逻辑契约为主。优先实现可在 Node 测的纯分支：

assert.equal(typeof applyEinkMode, 'function')
assert.equal(typeof isEinkModeActive, 'function')
```

（完整 DOM 行为在实现后用浏览器手测；若环境有 `globalThis.document`，则：）

```ts
if (typeof document !== 'undefined') {
  applyEinkMode(true)
  assert.equal(document.documentElement.dataset.eink, '1')
  assert.equal(isEinkModeActive(), true)
  applyEinkMode(false)
  assert.equal(document.documentElement.dataset.eink, undefined)
  assert.equal(isEinkModeActive(), false)
}
```

- [ ] **Step 2: 跑测确认失败**

Run: `npm run test:eink`  
Expected: FAIL（无法解析 `../src/lib/eink`）

- [ ] **Step 3: 实现 `src/lib/eink.ts`**

```ts
/** 墨水屏模式 DOM 标记；与 data-theme 正交，只表达行为叠加 */
export function applyEinkMode(enabled: boolean): void {
  const root = document.documentElement
  if (enabled) {
    root.dataset.eink = '1'
  } else {
    delete root.dataset.eink
  }
}

export function isEinkModeActive(): boolean {
  return document.documentElement.dataset.eink === '1'
}
```

- [ ] **Step 4: `index.html` 防闪**

在现有 preferences IIFE 内、设置 `dataset.theme` 之后增加：

```js
if (saved && saved.einkMode === true) {
  document.documentElement.dataset.eink = '1'
} else {
  delete document.documentElement.dataset.eink
}
```

- [ ] **Step 5: `usePreferences` 同步**

在 `useEffect`（save/typography/proxy）中增加 `applyEinkMode(Boolean(prefs.einkMode))`。

主题 effect 中：

```ts
const resolved = applyTheme(prefs.theme, {
  animate: themeApplied.current && !prefs.einkMode,
})
```

依赖数组改为 `[prefs.theme, prefs.einkMode]`（eink 开关变化时若主题未变也需重跑以去掉过渡类语义；若担心多余 applyTheme，可拆：仅 theme effect 读 `prefs.einkMode` 决定 animate，另 effect 只 `applyEinkMode`）。

推荐拆成两个 effect，避免主题逻辑搅在一起：

```ts
useEffect(() => {
  applyEinkMode(Boolean(prefs.einkMode))
}, [prefs.einkMode])

useEffect(() => {
  const sync = () => {
    const resolved = applyTheme(prefs.theme, {
      animate: themeApplied.current && !prefs.einkMode,
    })
    themeApplied.current = true
    setResolvedTheme(resolved)
    void applyNativeChrome(resolved)
  }
  sync()
  return prefs.theme === 'system' ? watchSystemTheme(sync) : undefined
}, [prefs.theme, prefs.einkMode])
```

- [ ] **Step 6: 跑测 + Commit**

```bash
npm run test:eink
git add src/lib/eink.ts index.html src/hooks/usePreferences.ts scripts/eink-mode.test.ts
git commit -m "feat(eink): apply data-eink from preferences and boot script"
```

---

### Task 3: `useReducedMotion` 合并 eink + CSS 装饰/动效降级

**Files:**
- Modify: `src/hooks/useReducedMotion.ts`
- Modify: `src/index.css`（在现有 `@media (prefers-reduced-motion: reduce)` 旁增加 `[data-eink='1']` 块）

**Interfaces:**
- Consumes: `data-eink`（MutationObserver）
- Produces: `useReducedMotion(): boolean` — `systemReduce || einkActive`；**无 eink 时与改前完全一致**

- [ ] **Step 1: 扩展 hook**

```ts
import { useEffect, useState } from 'react'

const QUERY = '(prefers-reduced-motion: reduce)'

function readEink(): boolean {
  return typeof document !== 'undefined' && document.documentElement.dataset.eink === '1'
}

export function useReducedMotion(): boolean {
  const [reduced, setReduced] = useState(
    () => typeof window !== 'undefined' && window.matchMedia(QUERY).matches,
  )
  const [eink, setEink] = useState(readEink)

  useEffect(() => {
    const media = window.matchMedia(QUERY)
    const onChange = () => setReduced(media.matches)
    media.addEventListener('change', onChange)
    return () => media.removeEventListener('change', onChange)
  }, [])

  useEffect(() => {
    const root = document.documentElement
    const sync = () => setEink(root.dataset.eink === '1')
    sync()
    const obs = new MutationObserver(sync)
    obs.observe(root, { attributes: true, attributeFilter: ['data-eink'] })
    return () => obs.disconnect()
  }, [])

  return reduced || eink
}
```

- [ ] **Step 2: CSS `[data-eink='1']`**

在 `src/index.css` 末尾（或 reduce-motion 媒体查询旁）增加：

```css
/* 墨水屏模式：与 prefers-reduced-motion 同等压制动效，并弱化装饰（不改亮暗色板） */
html[data-eink='1'] {
  --shadow-lift: none;
  --grain-opacity: 0;
  scroll-behavior: auto;
}

html[data-eink='1'] *,
html[data-eink='1'] *::before,
html[data-eink='1'] *::after {
  animation-duration: 0.01ms !important;
  animation-iteration-count: 1 !important;
  transition-duration: 0.01ms !important;
  box-shadow: none !important;
  text-shadow: none !important;
  backdrop-filter: none !important;
  -webkit-backdrop-filter: none !important;
}
```

说明：半透明底变实色若仅靠去 blur 不够，可在同块追加对已知壳层的实色覆盖（如 `.tab-bar` / 实际 class）。**优先用 token**；仅对仍透出底层的关键壳（`TabBar` 的 `bg-ink/92`）在 eink 下用额外规则改为 `bg-ink`。实现时在 `index.css` 用属性选择器：

```css
html[data-eink='1'] nav.relative.z-20 {
  /* 若 TabBar 无稳定 class，给 TabBar 根节点加 data-chrome="tabbar" 再写规则，避免脆弱选择器 */
}
```

更稳：给 `TabBar` 根元素加 `data-surface="tabbar"`，CSS：

```css
html[data-eink='1'] [data-surface='tabbar'] {
  background-color: var(--color-ink);
  backdrop-filter: none;
}
```

对 Reader 顶栏 pill 等半透明层同样加 `data-surface` 或复用已有 class，**仅在 `data-eink` 下覆盖**。

- [ ] **Step 3: 手测门闩（正常模式）**

确认 **未** 开 eink：`document.documentElement.dataset.eink` 不存在；列表入场、主题切换过渡仍在。

- [ ] **Step 4: Commit**

```bash
git add src/hooks/useReducedMotion.ts src/index.css src/components/TabBar.tsx
git commit -m "feat(eink): force reduced motion and strip decorative effects"
```

---

### Task 4: 外观设置开关 + App 接线

**Files:**
- Modify: `src/screens/settings/AppearanceScreen.tsx`
- Modify: `src/App.tsx`

**Interfaces:**
- Consumes: `setEinkMode` from preferences；`ToggleSwitch`
- Produces: 用户可切换 `prefs.einkMode`

- [ ] **Step 1: 扩展 AppearanceScreen props**

```ts
interface Props {
  theme: ThemeMode
  resolved: ResolvedTheme
  einkMode: boolean
  onChange: (theme: ThemeMode) => void
  onEinkModeChange: (enabled: boolean) => void
  onBack: () => void
}
```

在「主题」`SettingsSection` 与「启动」之间插入：

```tsx
<SettingsSection title="墨水屏">
  <div className="page-x">
    <div className="flex items-center justify-between gap-4 rounded-2xl border border-haze bg-ink-raised p-4 shadow-[var(--shadow-lift)]">
      <div className="min-w-0 flex-1">
        <span className="font-display text-[15px] font-medium text-paper">墨水屏模式</span>
        <p className="mt-1 text-[12px] leading-relaxed text-paper-muted">
          关闭动画与装饰效果，文章改为左右点击翻页。颜色仍跟随上方主题。关闭后恢复原有阅读与动效。
        </p>
      </div>
      <ToggleSwitch
        checked={einkMode}
        label="墨水屏模式"
        onChange={() => onEinkModeChange(!einkMode)}
      />
    </div>
  </div>
</SettingsSection>
```

导入 `ToggleSwitch`。

- [ ] **Step 2: App 接线**

`AppearanceScreen` 调用处传入：

```tsx
einkMode={Boolean(prefs.einkMode)}
onEinkModeChange={(enabled) => update((p) => setEinkMode(p, enabled))}
```

并从 `./sources/preferences` 导入 `setEinkMode`。

- [ ] **Step 3: 手测**

外观页开关 → `localStorage` / prefs 中 `einkMode` 为 true → `<html data-eink="1">`；关闭后属性消失，主题切换过渡恢复。

- [ ] **Step 4: Commit**

```bash
git add src/screens/settings/AppearanceScreen.tsx src/App.tsx
git commit -m "feat(settings): add eink mode toggle on appearance screen"
```

---

### Task 5: 分页纯函数 `readerPagination.ts`

**Files:**
- Create: `src/lib/readerPagination.ts`
- Modify: `scripts/eink-mode.test.ts`

**Interfaces:**
- Produces:
  - `PageTapZone = 'prev' | 'toggleChrome' | 'next'`
  - `resolvePageTapZone(clientX: number, width: number, leftRatio?: number, rightRatio?: number): PageTapZone`
  - `PageSlice = { startOffset: number; endOffset: number }`
  - `paginateOffsets(blockEnds: number[], pageHeight: number): PageSlice[]`  
    - `blockEnds[i]` = 第 i 块底部相对内容顶的 offset（单调递增）
    - 装页：尽量不把块切开；单块高度 > pageHeight 时该块独占一页（slice 高度可为块高，UI 页内可滚）
  - `findPageIndex(pages: PageSlice[], offset: number): number` — 偏移落在哪一页
  - `clampPageIndex(index: number, pageCount: number): number`

- [ ] **Step 1: 写失败单测**

追加到 `scripts/eink-mode.test.ts`：

```ts
import {
  clampPageIndex,
  findPageIndex,
  paginateOffsets,
  resolvePageTapZone,
} from '../src/lib/readerPagination'

assert.equal(resolvePageTapZone(10, 100), 'prev')
assert.equal(resolvePageTapZone(50, 100), 'toggleChrome')
assert.equal(resolvePageTapZone(90, 100), 'next')
assert.equal(resolvePageTapZone(0, 0), 'toggleChrome')

// blocks ends at 100,200,350,400；pageHeight 150
// page0: 0-200 (两块), page1: 200-350, page2: 350-400
const pages = paginateOffsets([100, 200, 350, 400], 150)
assert.deepEqual(pages[0], { startOffset: 0, endOffset: 200 })
assert.ok(pages.length >= 2)

assert.equal(findPageIndex(pages, 0), 0)
assert.equal(findPageIndex(pages, pages[0].endOffset), Math.min(1, pages.length - 1))

assert.equal(clampPageIndex(-1, 3), 0)
assert.equal(clampPageIndex(9, 3), 2)
assert.equal(clampPageIndex(1, 0), 0)

console.log('eink-mode pagination: ok')
```

（`paginateOffsets` 精确期望以实现注释为准；单测在实现后按「不切块、超高块独占」锁定断言。）

- [ ] **Step 2: 跑测失败 → 实现**

`src/lib/readerPagination.ts`：

```ts
export type PageTapZone = 'prev' | 'toggleChrome' | 'next'

export function resolvePageTapZone(
  clientX: number,
  width: number,
  leftRatio = 0.28,
  rightRatio = 0.28,
): PageTapZone {
  if (width <= 0) return 'toggleChrome'
  const x = Math.min(Math.max(clientX, 0), width)
  if (x < width * leftRatio) return 'prev'
  if (x > width * (1 - rightRatio)) return 'next'
  return 'toggleChrome'
}

export interface PageSlice {
  startOffset: number
  endOffset: number
}

/** blockEnds: 每块底部 y；pageHeight: 一页可视高度 */
export function paginateOffsets(blockEnds: number[], pageHeight: number): PageSlice[] {
  if (pageHeight <= 0) return [{ startOffset: 0, endOffset: 0 }]
  if (!blockEnds.length) return [{ startOffset: 0, endOffset: 0 }]

  const pages: PageSlice[] = []
  let start = 0
  let startIdx = 0

  for (let i = 0; i < blockEnds.length; i++) {
    const end = blockEnds[i]
    const prevEnd = i === 0 ? 0 : blockEnds[i - 1]
    const blockHeight = end - prevEnd

    if (blockHeight > pageHeight) {
      if (i > startIdx) {
        pages.push({ startOffset: start, endOffset: prevEnd })
      }
      pages.push({ startOffset: prevEnd, endOffset: end })
      start = end
      startIdx = i + 1
      continue
    }

    if (end - start > pageHeight && i > startIdx) {
      pages.push({ startOffset: start, endOffset: prevEnd })
      start = prevEnd
      startIdx = i
    }
  }

  const lastEnd = blockEnds[blockEnds.length - 1]
  if (startIdx <= blockEnds.length - 1 || pages.length === 0) {
    if (!pages.length || pages[pages.length - 1].endOffset !== lastEnd) {
      pages.push({ startOffset: start, endOffset: lastEnd })
    }
  }

  return pages.length ? pages : [{ startOffset: 0, endOffset: lastEnd }]
}

export function findPageIndex(pages: PageSlice[], offset: number): number {
  if (!pages.length) return 0
  for (let i = 0; i < pages.length; i++) {
    if (offset < pages[i].endOffset || i === pages.length - 1) return i
  }
  return pages.length - 1
}

export function clampPageIndex(index: number, pageCount: number): number {
  if (pageCount <= 0) return 0
  return Math.min(Math.max(index, 0), pageCount - 1)
}
```

实现后按真实装页结果修正单测期望值，保证「不切普通块、超高块独占」不变量。

- [ ] **Step 3: 跑通 `npm run test:eink` → Commit**

```bash
git add src/lib/readerPagination.ts scripts/eink-mode.test.ts
git commit -m "feat(reader): add pagination pure helpers for eink mode"
```

---

### Task 6: Reader 分页模式分支（完整交付）

**Files:**
- Modify: `src/screens/ReaderScreen.tsx`
- Modify: `src/App.tsx`（传入 `einkMode`）
- 可选 Create: `src/hooks/usePagedReader.ts`（测量 + 页码 state，保持 ReaderScreen 可读）

**Interfaces:**
- Consumes: `einkMode: boolean`；`resolvePageTapZone` / `paginateOffsets` / `findPageIndex` / `clampPageIndex`
- Produces: eink 下分页 UI；`einkMode=false` 时 **现有 JSX/滚动逻辑原样保留**

- [ ] **Step 1: Props**

```ts
interface Props {
  // ...existing
  einkMode?: boolean
}
```

App：

```tsx
<ReaderScreen
  ...
  einkMode={Boolean(prefs.einkMode)}
/>
```

- [ ] **Step 2: 分页状态与测量（仅 `einkMode`）**

当 `einkMode`：

1. 内容滚动容器改为 `overflow-hidden`（非 `overflow-y-auto`）；内容包裹层 `transform: translateY(-page.startOffset)`，高度裁切为 `pageHeight`（或外层 clip）。
2. `ResizeObserver` + 正文 `ready` 后测量 `.reader-prose`（及标题区）子块 `offsetTop+offsetHeight`，得到 `blockEnds`，调用 `paginateOffsets`。
3. `pageIndex` state；顶栏可见时显示 `当前/总页`。
4. **停用** `handleScroll` pill 逻辑；改为中区点击 `setPillVisible(v => !v)`。
5. 透明点击层（三区或整层 pointer 根据坐标 `resolvePageTapZone`）：
   - 若 `event.target.closest('a, button, [role="button"], input, video, [data-no-page-tap]')` → 不翻页
   - lightbox / commentsOpen / 翻译错误条内交互 → 不翻页
6. 左缘返回：`useEdgeSwipeBack` 保持；点击分区左缘窄于 swipe edge 时仍可返回（现有 edge width）。
7. 超高页：该 `PageSlice` 高度 > viewport 时，**仅该页**内容区允许 `overflow-y-auto`。
8. 会话进度：`sessionStorage` 键 `newsnook:eink-page:${article.id}` 存页码；进入时读取；`einkMode=false` **不读不写**。
9. 阅读中 `einkMode` false→true：用当前 `scrollTop` → `findPageIndex`；true→false：用当前页 `startOffset` 赋回 `scrollTop`，并确保滚动容器 class 恢复。

**关键：用 `if (einkMode) { ... } else { ... }` 或早期分支渲染两套容器属性，禁止改写非 eink 路径的默认 class/handler 语义。**

- [ ] **Step 3: 手测清单**

**正常模式（eink 关）：**

- [ ] Reader 仍上下滚动；pill 随滚动显隐
- [ ] 边缘返回、图/视频、翻译、评论正常
- [ ] 主题切换仍有过渡（系统未开 reduce-motion 时）

**墨水屏开：**

- [ ] 左/中/右点击行为正确；链接不误触翻页
- [ ] 翻页无动画；页码显示
- [ ] 改字号后重分页
- [ ] 图/视频仍可打开

**往返：**

- [ ] 关 eink 后立即恢复滚动与动效；无 `data-eink`；无透明翻页层

- [ ] **Step 4: Commit**

```bash
git add src/screens/ReaderScreen.tsx src/App.tsx src/hooks/usePagedReader.ts
git commit -m "feat(reader): paginated reading when eink mode is on"
```

---

### Task 7: 回归加固与规格对齐检查

**Files:**
- Modify: `scripts/eink-mode.test.ts`（补齐边界）
- 必要时微调 CSS / Reader

- [ ] **Step 1: 补测**

- `normalizePreferences` 与 `setEinkMode` 不改写 `theme` / `typography`
- `paginateOffsets([])`、单块超高、刚好一页多块
- `resolvePageTapZone` 边界（恰好 28% / 72%）

- [ ] **Step 2: `npm run test:eink` + `npm run lint` + `npm run build`**

Expected: 全部通过

- [ ] **Step 3: 对照规格逐条勾选**

规格章节 → 任务映射：

| 规格 | 任务 |
|---|---|
| §3 零回归 | Task 6/7 手测 + 默认 false |
| §4 偏好与运行时 | Task 1–2 |
| §5 动效与装饰 | Task 3 |
| §6 分页阅读 | Task 5–6 |
| §7 设置文案 | Task 4 |
| §9 测试 | Task 1/5/7 + 手测 |
| 媒体不变 | 无改 InkImage/InkVideoPlayer 策略 |

- [ ] **Step 4: Commit**

```bash
git add scripts/eink-mode.test.ts
git commit -m "test(eink): harden pagination and preference edge cases"
```

---

## Self-Review (plan vs spec)

1. **Spec coverage:** 偏好、DOM 标记、防闪、reduce-motion、CSS 装饰、设置开关、分页纯函数、Reader 分支、零回归手测均有任务。媒体策略明确不改。
2. **Placeholders:** 无 TBD；TabBar 实色通过 `data-surface` 落地，避免脆弱选择器。
3. **Type consistency:** `einkMode: boolean`、`applyEinkMode`、`setEinkMode`、`PageSlice` / `resolvePageTapZone` 命名贯穿 Task 1–6。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-08-11-eink-mode.md`.

**Two execution options:**

1. **Subagent-Driven (recommended)** — 每任务新开子代理，任务间审查  
2. **Inline Execution** — 本会话按 executing-plans 连续推进并设检查点  

Which approach?
